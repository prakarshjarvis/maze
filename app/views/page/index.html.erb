<h1>Hi! This is Maze</h1>
<p>Welcome to maze, you can connect with your friends by creating some posts here.</p>
<div>
  <% if @user.has_role? :admin %>
    <h2> UserList: </h2>
    <ul class="list-group">
      <% @users.each do |user| %>
        <li class="list-group-item">
          <%= render 'userList', user: user, current_user: @user %>
        </li>
      <% end %>
    </ul>
    <%= link_to 'Add Users', page_new_path, class: "btn btn-secondary" %>
    <%= link_to "Dowload Users CSV", users_admin_index_path(format: :csv), class: "btn btn-secondary" %>
    <%#= render partial: 'newUser', user: @newUser,  class: "btn btn-secondary" %>
  <% end %>
</div>
</br>
<div>
  <%= link_to 'View Posts', posts_path(id: current_user.id), class: "btn btn-secondary" %>
  <%= link_to "Sign Out", destroy_user_session_path, class: "btn btn-secondary" %>
</div>


