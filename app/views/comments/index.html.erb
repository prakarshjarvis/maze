<p style="color: green"><%= notice %></p>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <%= render comment %>
  <% end %>
  <%= link_to "New comments", new_comment_path(id: @post.id), class: 'btn btn-secondary' %>
</div>
