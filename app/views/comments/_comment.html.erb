<div id="<%= dom_id comment %>" style="display: flex; margin-top: 5%">
  <div style="margin-right: 8%">
    <strong><%= comment.user.firstname %></strong>
    <strong><%= comment.user.lastname %></strong>
    : <%= comment.body %>
  </div>
    <%= render partial: 'likes/button', locals: {likeable: comment} %>
</div>